post_install() {
    if ! grep -q "/usr/bin/crush" /etc/shells; then
        echo "/usr/bin/crush" >> /etc/shells
    fi
}

post_upgrade() {
    post_install
}

pre_remove() {
    sed -i -e '\|^/usr/bin/crush$|d' /etc/shells
}
